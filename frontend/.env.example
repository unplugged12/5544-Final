# API base URL - leave empty to use Vite proxy (recommended for dev)
# For direct access without proxy, use http://localhost:18000
# NEVER use Docker service names (e.g. http://backend:8000) - this runs in the browser
VITE_API_URL=
